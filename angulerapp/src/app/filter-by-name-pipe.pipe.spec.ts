import { FilterByNamePipePipe } from './filter-by-name-pipe.pipe';

describe('FilterByNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
